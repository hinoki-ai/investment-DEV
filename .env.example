# =============================================================================
# FAMILY INVESTMENT DASHBOARD - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------
ENVIRONMENT=development
PROJECT_NAME="Family Investment Dashboard"

# -----------------------------------------------------------------------------
# Database (Layer 2: Coordination)
# -----------------------------------------------------------------------------
DB_USER=investor
DB_PASSWORD=family_future_2024
DB_NAME=investments
DB_PORT=5432
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}

# -----------------------------------------------------------------------------
# Redis (Cache & Queue)
# -----------------------------------------------------------------------------
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# Storage (Layer 1: File Storage)
# For local dev: MinIO
# For production: Cloudflare R2, AWS S3, etc.
# -----------------------------------------------------------------------------
# Local MinIO (Development)
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin
STORAGE_BUCKET=investments
STORAGE_REGION=us-east-1

# Production: Cloudflare R2 (uncomment when ready)
# STORAGE_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# STORAGE_ACCESS_KEY=your-r2-access-key
# STORAGE_SECRET_KEY=your-r2-secret-key
# STORAGE_BUCKET=family-investments
# STORAGE_REGION=auto

# -----------------------------------------------------------------------------
# API Server
# -----------------------------------------------------------------------------
API_PORT=8000
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# -----------------------------------------------------------------------------
# AI Provider (Layer 3: Intelligence)
# Supports: openai, anthropic, google, kimi (moonshot)
# -----------------------------------------------------------------------------

# Set your provider (auto-detected from API keys if not set)
# AI_PROVIDER=openai  # Options: openai, anthropic, google, kimi

# Generic AI settings (works for any OpenAI-compatible API)
AI_API_KEY=your-api-key-here
AI_API_URL=https://api.openai.com/v1  # Or your provider's endpoint
AI_MODEL=gpt-4o  # Or claude-3-5-sonnet, gemini-1.5-flash, etc.

# Provider-specific settings (alternative to generic settings above)
# These auto-set AI_PROVIDER if AI_PROVIDER is not explicitly set

# --- Kimi (Moonshot AI) ---
# KIMI_API_KEY=your-kimi-api-key
# KIMI_API_URL=https://api.moonshot.cn/v1
# KIMI_MODEL=kimi-k2-5

# --- OpenAI ---
# OPENAI_API_KEY=your-openai-key
# OPENAI_API_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o

# --- Anthropic Claude ---
# ANTHROPIC_API_KEY=your-anthropic-key
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# --- Google Gemini ---
# GOOGLE_API_KEY=your-google-key
# GEMINI_API_KEY=your-gemini-key  # Alternative env var
# GOOGLE_MODEL=gemini-1.5-flash

# --- Local Models (Ollama, vLLM, etc.) ---
# AI_PROVIDER=openai
# AI_API_URL=http://localhost:11434/v1  # Ollama default
# AI_MODEL=llava  # Or any vision-capable model

# -----------------------------------------------------------------------------
# Worker Configuration
# -----------------------------------------------------------------------------
WORKER_POLL_INTERVAL=10
MAX_CONCURRENT_JOBS=3

# -----------------------------------------------------------------------------
# Web Dashboard
# -----------------------------------------------------------------------------
WEB_PORT=5173
VITE_API_URL=http://localhost:8000
